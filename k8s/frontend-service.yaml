apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: zero-trust-lab
  labels:
    app: frontend
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
